import 'package:flutter/material.dart';

class DinnerListScreen extends StatelessWidget {
  const DinnerListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Dinner")),
    );
  }
}
